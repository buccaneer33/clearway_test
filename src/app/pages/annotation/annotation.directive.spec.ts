import { AnnotationDirective } from './annotation.directive';

describe('ContextDirective', () => {
  it('should create an instance', () => {
    // const directive = new AnnotationDirective();
    // expect(directive).toBeTruthy();
  });
});
